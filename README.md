# Disk Analyzer

Небольшой инструмент для сбора информации о дисковом пространстве и создания визуализации на её основе.
Визуализирует данные через приложение, написанное на Custom Tkinter.
Предоставляет полностью графический интерфейс.

## 🚀 Быстрый старт

Скачайте последнюю из ![релизов](https://github.com/Aetorny/Disk-Analyzer/releases)
На данный момент собраны только версии под Windows

## 📷 Скриншоты

Ниже приведены примеры интерфейса приложения и функций визуализатора:

![Выбор диска](readme/choose_disk.png)

Папки и файлы окрашены в тепловую карту, в зависимости от того, сколько места на диске они занимают:

![Главное окно](readme/main_window.png)

![Поиск файлов](readme/search.png)

## Запуск из исходного кода

Требования:
- **Python 3.14+**
- `pip` (обычно входит в комплект Python)

Рекомендуется использовать Python 3.14t (версия с отключенным GIL) для наибольшей скорости работы.

1. Клонируйте репозиторий или скачайте архив и перейдите в папку проекта:

```powershell
git clone https://github.com/Aetorny/Disk-Analyzer
cd ".\Disk-Analyzer\"
```

2. (Рекомендуется) создайте виртуальное окружение и активируйте его (PowerShell):

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.bat
```

3. Установите зависимости:

```powershell
pip install -r .\requirements.txt
```

4. Запустите анализ и генерацию визуализаций:

```powershell
python main.py
```

## 📂 Результат

Генерируемые файлы будут лежать в папке `data/`, например:

- `data/disk_usage_C.data`

## 🛠 Отладка и примечания

- Для пересоздания отчётов запустите приложение заново и вновь просканируйте необходимые диски.

## Лицензия

Этот проект распространяется под **MIT License** — см. файл `LICENSE` в корне репозитория для полного текста.

Кратко: вы можете использовать, копировать, изменять и распространять код, в том числе в коммерческих проектах, при условии сохранения уведомления об авторских правах и данного разрешения (атрибуция авторства).
